import numpy as np

pares = np.arange(2, 51, 2)
impares = np.arange(1, 50, 2)
desc = np.arange(10, 0, -1)

print("Pares:", pares, "len:", len(pares))
print("Impares:", impares, "len:", len(impares))
print("10 a 1:", desc, "len:", len(desc))
